<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:Bellwood.DriverApp.ViewModels"
             xmlns:helpers="clr-namespace:Bellwood.DriverApp.Helpers"
             x:Class="Bellwood.DriverApp.Views.RideDetailPage"
             x:DataType="viewmodels:RideDetailViewModel"
             Title="{Binding Title}">

    <ContentPage.Resources>
        <helpers:BoolToGpsIconConverter x:Key="BoolToGpsIconConverter"/>
    </ContentPage.Resources>

    <ScrollView>
        <VerticalStackLayout Spacing="0" Padding="0">

            <!-- Header Card -->
            <Frame Margin="0" 
                   Padding="20" 
                   BackgroundColor="{StaticResource Primary}"
                   CornerRadius="0"
                   HasShadow="False">
                <VerticalStackLayout Spacing="10">
                    <Label Text="{Binding Ride.PassengerName}"
                           FontSize="24"
                           FontAttributes="Bold"
                           TextColor="White"/>
                    <Label Text="{Binding Ride.DisplayPickupTime, StringFormat='Pick up: {0:MMM dd, yyyy h:mm tt}'}"
                           FontSize="16"
                           TextColor="White"/>
                    <Label Text="{Binding Ride.Status}"
                           FontSize="18"
                           FontAttributes="Bold"
                           TextColor="White"
                           Margin="0,5,0,0"/>
                </VerticalStackLayout>
            </Frame>

            <!-- Enhanced Location Tracking Indicator -->
            <Frame IsVisible="{Binding ShowTrackingIndicator}"
                   Margin="10"
                   Padding="12"
                   CornerRadius="8"
                   HasShadow="False"
                   BackgroundColor="{StaticResource Gray400}">
                <Frame.Triggers>
                    <DataTrigger TargetType="Frame" Binding="{Binding HasTrackingError}" Value="True">
                        <Setter Property="BackgroundColor" Value="{StaticResource Warning}"/>
                    </DataTrigger>
                    <DataTrigger TargetType="Frame" Binding="{Binding IsTracking}" Value="True">
                        <Setter Property="BackgroundColor" Value="{StaticResource Success}"/>
                    </DataTrigger>
                </Frame.Triggers>
                <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="10">
                    <!-- GPS Icon -->
                    <Label Grid.Column="0" 
                           Text="{Binding IsTracking, Converter={StaticResource BoolToGpsIconConverter}}"
                           FontSize="18"
                           VerticalOptions="Center"/>
                    
                    <!-- Status Message -->
                    <VerticalStackLayout Grid.Column="1" VerticalOptions="Center">
                        <Label Text="{Binding TrackingStatusMessage}"
                               TextColor="White"
                               FontAttributes="Bold"
                               FontSize="14"/>
                        <Label Text="Your location is visible to the passenger"
                               TextColor="White"
                               FontSize="11"
                               Opacity="0.85"
                               IsVisible="{Binding IsTracking}"/>
                    </VerticalStackLayout>
                    
                    <!-- Error indicator -->
                    <Label Grid.Column="2"
                           Text="⚠️"
                           FontSize="18"
                           VerticalOptions="Center"
                           IsVisible="{Binding HasTrackingError}"/>
                </Grid>
            </Frame>

            <!-- Passenger Info -->
            <Frame Margin="10" Padding="15" BackgroundColor="White" CornerRadius="8">
                <VerticalStackLayout Spacing="10">
                    <Label Text="Passenger Information"
                           FontSize="18"
                           FontAttributes="Bold"
                           TextColor="{StaticResource Gray900}"/>
                    
                    <HorizontalStackLayout Spacing="10">
                        <Label Text="👤" FontSize="16"/>
                        <Label Text="{Binding Ride.PassengerName}"
                               FontSize="16"
                               TextColor="{StaticResource Gray700}"/>
                    </HorizontalStackLayout>

                    <HorizontalStackLayout Spacing="10">
                        <Label Text="👥" FontSize="16"/>
                        <Label Text="{Binding Ride.PassengerCount, StringFormat='{0} passenger(s)'}"
                               FontSize="16"
                               TextColor="{StaticResource Gray700}"/>
                    </HorizontalStackLayout>

                    <!-- Phone: prefer SavedPassengerPhone (from dispatcher's saved record),
                         fall back to PassengerPhone supplied directly on the booking -->
                    <HorizontalStackLayout Spacing="10"
                                          IsVisible="{Binding Ride.SavedPassengerPhone, Converter={StaticResource IsNotNullConverter}}">
                        <Label Text="📞" FontSize="16"/>
                        <Label Text="{Binding Ride.SavedPassengerPhone}"
                               FontSize="16"
                               TextColor="{StaticResource Gray700}"/>
                    </HorizontalStackLayout>

                    <HorizontalStackLayout Spacing="10"
                                          IsVisible="{Binding Ride.PassengerPhone, Converter={StaticResource IsNotNullConverter}}">
                        <Label Text="📞" FontSize="16"/>
                        <Label Text="{Binding Ride.PassengerPhone}"
                               FontSize="16"
                               TextColor="{StaticResource Gray700}"/>
                    </HorizontalStackLayout>

                    <!-- Email from saved passenger record -->
                    <HorizontalStackLayout Spacing="10"
                                          IsVisible="{Binding Ride.SavedPassengerEmail, Converter={StaticResource IsNotNullConverter}}">
                        <Label Text="✉️" FontSize="16"/>
                        <Label Text="{Binding Ride.SavedPassengerEmail}"
                               FontSize="16"
                               TextColor="{StaticResource Gray700}"/>
                    </HorizontalStackLayout>
                </VerticalStackLayout>
            </Frame>

            <!-- Locations -->
            <Frame Margin="10" Padding="15" BackgroundColor="White" CornerRadius="8">
                <VerticalStackLayout Spacing="15">
                    <Label Text="Locations"
                           FontSize="18"
                           FontAttributes="Bold"
                           TextColor="{StaticResource Gray900}"/>

                    <!-- Pickup -->
                    <VerticalStackLayout Spacing="5">
                        <Label Text="PICK UP" 
                               FontSize="12" 
                               TextColor="{StaticResource Gray500}"
                               FontAttributes="Bold"/>
                        <!-- Saved location label (dispatcher-supplied) -->
                        <Label Text="{Binding Ride.SavedPickupLocationLabel}"
                               FontSize="14"
                               FontAttributes="Bold"
                               TextColor="{StaticResource Primary}"
                               IsVisible="{Binding Ride.SavedPickupLocationLabel, Converter={StaticResource IsNotNullConverter}}"/>
                        <Label Text="{Binding Ride.PickupLocation}"
                               FontSize="16"
                               TextColor="{StaticResource Gray900}"/>
                        <Label Text="{Binding Ride.PickupStyle}"
                               FontSize="14"
                               TextColor="{StaticResource Gray600}"
                               IsVisible="{Binding Ride.PickupStyle, Converter={StaticResource IsNotNullConverter}}"/>
                        <Label Text="{Binding Ride.PickupSignText, StringFormat='Sign: {0}'}"
                               FontSize="14"
                               TextColor="{StaticResource Info}"
                               FontAttributes="Bold"
                               IsVisible="{Binding Ride.PickupSignText, Converter={StaticResource IsNotNullConverter}}"/>
                        <Button Text="Navigate to Pick Up"
                                Command="{Binding NavigateToPickupCommand}"
                                BackgroundColor="{StaticResource Primary}"
                                TextColor="White"
                                CornerRadius="8"
                                Margin="0,5,0,0"/>
                    </VerticalStackLayout>

                    <BoxView HeightRequest="1" Color="{StaticResource Gray200}" Margin="0,5"/>

                    <!-- Dropoff -->
                    <VerticalStackLayout Spacing="5">
                        <Label Text="DROP OFF" 
                               FontSize="12" 
                               TextColor="{StaticResource Gray500}"
                               FontAttributes="Bold"/>
                        <!-- Saved location label (dispatcher-supplied) -->
                        <Label Text="{Binding Ride.SavedDropoffLocationLabel}"
                               FontSize="14"
                               FontAttributes="Bold"
                               TextColor="{StaticResource Primary}"
                               IsVisible="{Binding Ride.SavedDropoffLocationLabel, Converter={StaticResource IsNotNullConverter}}"/>
                        <Label Text="{Binding Ride.DropoffLocation, TargetNullValue='As Directed'}"
                               FontSize="16"
                               TextColor="{StaticResource Gray900}"/>
                        <Button Text="Navigate to Drop Off"
                                Command="{Binding NavigateToDropoffCommand}"
                                IsVisible="{Binding Ride.DropoffLocation, Converter={StaticResource IsNotNullConverter}}"
                                BackgroundColor="{StaticResource Primary}"
                                TextColor="White"
                                CornerRadius="8"
                                Margin="0,5,0,0"/>
                    </VerticalStackLayout>
                </VerticalStackLayout>
            </Frame>

            <!-- Ride Details -->
            <Frame Margin="10" Padding="15" BackgroundColor="White" CornerRadius="8">
                <VerticalStackLayout Spacing="10">
                    <Label Text="Ride Details"
                           FontSize="18"
                           FontAttributes="Bold"
                           TextColor="{StaticResource Gray900}"/>

                    <Grid ColumnDefinitions="*,*" RowDefinitions="Auto,Auto" RowSpacing="8" ColumnSpacing="10">
                        <Label Grid.Row="0" Grid.Column="0"
                               Text="{Binding Ride.VehicleClass, StringFormat='🚗 {0}'}"
                               FontSize="14"
                               TextColor="{StaticResource Gray900}"/>
                        <Label Grid.Row="0" Grid.Column="1"
                               Text="{Binding Ride.PassengerCount, StringFormat='👥 {0} passenger(s)'}"
                               FontSize="14"
                               TextColor="{StaticResource Gray900}"/>
                        <Label Grid.Row="1" Grid.Column="0"
                               Text="{Binding Ride.CheckedBags, StringFormat='🧳 {0} checked bag(s)'}"
                               FontSize="14"
                               TextColor="{StaticResource Gray900}"/>
                        <Label Grid.Row="1" Grid.Column="1"
                               Text="{Binding Ride.CarryOnBags, StringFormat='🎒 {0} carry-on(s)'}"
                               FontSize="14"
                               TextColor="{StaticResource Gray900}"/>
                    </Grid>

                    <!-- Flight Info -->
                    <Label Text="{Binding Ride.OutboundFlight.FlightNumber, StringFormat='✈️ Flight: {0}'}"
                           FontSize="14"
                           TextColor="{StaticResource Gray900}"
                           IsVisible="{Binding Ride.OutboundFlight.FlightNumber, Converter={StaticResource IsNotNullConverter}}"/>

                    <!-- Special Requests -->
                    <Frame IsVisible="{Binding Ride.AdditionalRequest, Converter={StaticResource IsNotNullConverter}}"
                           BackgroundColor="{StaticResource Warning}"
                           Padding="10"
                           CornerRadius="8"
                           HasShadow="False"
                           Margin="0,5,0,0">
                        <VerticalStackLayout Spacing="5">
                            <Label Text="📝 Special Request"
                                   FontSize="12"
                                   FontAttributes="Bold"
                                   TextColor="White"/>
                            <Label Text="{Binding Ride.AdditionalRequest}"
                                   FontSize="14"
                                   TextColor="White"/>
                        </VerticalStackLayout>
                    </Frame>

                    <!-- Dispatcher Notes (future: populated when dispatcher attaches a note) -->
                    <Frame IsVisible="{Binding Ride.DispatcherNotes, Converter={StaticResource IsNotNullConverter}}"
                           BackgroundColor="{StaticResource Info}"
                           Padding="10"
                           CornerRadius="8"
                           HasShadow="False"
                           Margin="0,5,0,0">
                        <VerticalStackLayout Spacing="5">
                            <Label Text="🗂️ Dispatcher Notes"
                                   FontSize="12"
                                   FontAttributes="Bold"
                                   TextColor="White"/>
                            <Label Text="{Binding Ride.DispatcherNotes}"
                                   FontSize="14"
                                   TextColor="White"/>
                        </VerticalStackLayout>
                    </Frame>
                </VerticalStackLayout>
            </Frame>

            <!-- Status Update Buttons -->
            <Frame Margin="10" Padding="15" BackgroundColor="White" CornerRadius="8">
                <VerticalStackLayout Spacing="10">
                    <Label Text="Update Ride Status"
                           FontSize="18"
                           FontAttributes="Bold"/>

                    <Button Text="▶️ Start Trip (On Route)"
                            Command="{Binding UpdateStatusCommand}"
                            CommandParameter="OnRoute"
                            IsVisible="{Binding CanTransitionToOnRoute}"
                            BackgroundColor="{StaticResource Primary}"
                            TextColor="White"
                            FontAttributes="Bold"
                            HeightRequest="50"
                            CornerRadius="8"/>

                    <Button Text="📍 Mark Arrived"
                            Command="{Binding UpdateStatusCommand}"
                            CommandParameter="Arrived"
                            IsVisible="{Binding CanTransitionToArrived}"
                            BackgroundColor="{StaticResource Info}"
                            TextColor="White"
                            FontAttributes="Bold"
                            HeightRequest="50"
                            CornerRadius="8"/>

                    <Button Text="🚗 Passenger Onboard"
                            Command="{Binding UpdateStatusCommand}"
                            CommandParameter="PassengerOnboard"
                            IsVisible="{Binding CanTransitionToPassengerOnboard}"
                            BackgroundColor="{StaticResource Success}"
                            TextColor="White"
                            FontAttributes="Bold"
                            HeightRequest="50"
                            CornerRadius="8"/>

                    <Button Text="✅ Complete Ride"
                            Command="{Binding UpdateStatusCommand}"
                            CommandParameter="Completed"
                            IsVisible="{Binding CanTransitionToCompleted}"
                            BackgroundColor="{StaticResource Success}"
                            TextColor="White"
                            FontAttributes="Bold"
                            HeightRequest="50"
                            CornerRadius="8"/>

                    <Button Text="❌ Cancel Ride"
                            Command="{Binding UpdateStatusCommand}"
                            CommandParameter="Cancelled"
                            IsVisible="{Binding CanCancel}"
                            BackgroundColor="{StaticResource Danger}"
                            TextColor="White"
                            FontAttributes="Bold"
                            HeightRequest="50"
                            CornerRadius="8"/>
                </VerticalStackLayout>
            </Frame>

            <!-- Error Message -->
            <Label Text="{Binding ErrorMessage}"
                   TextColor="{StaticResource Danger}"
                   FontSize="14"
                   IsVisible="{Binding ErrorMessage, Converter={StaticResource IsNotNullConverter}}"
                   Margin="20,10"
                   HorizontalOptions="Center"/>

            <!-- Loading Overlay -->
            <ActivityIndicator IsRunning="{Binding IsBusy}"
                              IsVisible="{Binding IsBusy}"
                              Color="{StaticResource Primary}"
                              Margin="20"/>

            <!-- Bottom spacing -->
            <BoxView HeightRequest="20"/>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
